#include <stdio.h>
#include <unistd.h>

int main() {
    // Print the process ID
    printf("Process ID: %d\n", getpid());
    
    // Infinite loop
    while(1) {
        // Do nothing, just loop
    }

    return 0;
}

